---
type: log
---

# 기록장

**이 파일은 기계가 씁니다. 편집하지 마세요.**

query·ingest·lint 가 무엇을 했는지 시간순으로 쌓입니다.
`query` 가 코드를 열어봤다는 건 **위키에 그 내용이 없었다**는 뜻이고,
그것이 다음에 무엇을 문서화할지 알려주는 가장 정직한 신호입니다.

`cw log <프로젝트> --gaps` 로 "위키에 없어서 코드를 열어본 질문"만 모아 볼 수 있습니다.

| 날짜 | 연산 | 내용 | 결과 |
|---|---|---|---|
